# Plan: static repo conversion + Tailscale mesh (MOVED)

This plan spans the whole fleet, not just this repo, so it moved to the fleet
meta-repo on 2026-07-15: `moquette/kodi` (private), at
`docs/static-repo-and-tailscale.md`. Locally that is
`~/Code/moquette/kodi/docs/static-repo-and-tailscale.md`.

Sequencing update recorded there the same day: the estuary7 Phase 5 fleet
skin migration is DROPPED (boxes switch manually at leisure), modv2plus is
DEPRECATED, and the static conversion no longer gates on any migration.

The original text as first committed here is in this file's git history
(commit `6a86c7e`).
