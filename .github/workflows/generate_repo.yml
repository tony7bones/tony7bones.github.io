name: Validate Kodi Repository

on:
  push:
    paths:
      - "repo/**/addon.xml"
      - "repo/media/**"
      - "repo/repositories/**"
      - "repo/scripts/**"
      - "repo/misc/**"
      - "_tools/generate_repo.py"
      - "_tools/test_generate_repo.py"
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install pytest

      - name: Run tests
        run: python3 -m pytest _tools/test_generate_repo.py -q

      - name: Verify generated files are up to date
        run: |
          python3 _tools/generate_repo.py
          if [ -n "$(git status --porcelain)" ]; then
            echo "Generated files are out of date — run generate_repo.py locally before committing."
            git status --short
            exit 1
          fi
